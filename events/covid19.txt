namespace = covid19

## Main Event
country_event = {
    id = covid19.1
    title = "covid19.1.title"
    desc = "covid19.1.description"
    picture = DEATH

    trigger = {
        is_year = 1444
        owns = 682
        # NOT = { has_country_flag = covid_outbreak }  # Uncomment this when done testing
    }

    mean_time_to_happen = {
        months = 1
    }

    immediate = {
        set_country_flag = covid_outbreak
        682 = { 
            set_province_flag = covid_origin
        }
    }

    option = {
        name = covid19.1.a  # Quarantine Province
        add_unrest = 5
        add_manpower = -3

        682 = {
            set_province_flag = infected_province
            add_province_modifier = {
                name = covid_local_quarantine
                duration = 180
            }
        }
    }

    option = {
        name = covid19.1.b  # Quarantine Country
        add_unrest = 3
        add_manpower = -10

        682 = {
            set_province_flag = infected_province
            add_province_modifier = {
                name = covid_local_quarantine
                duration = 180
            }
        }

        add_country_modifier = {
            name = covid_quarantine_nationwide
            duration = 365
        }
    }

    option = {
        name = covid19.1.c  # Ignore It
        add_unrest = 8
        add_manpower = -2
        add_stability = -1
        add_corruption = 1

        682 = {
            set_province_flag = infected_province
            add_province_modifier = {
                name = covid_local_quarantine
                duration = 180
            }
        }
    }
}
